import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['m.media-amazon.com', "lh3.googleusercontent.com", "avatars.slack-edge.com", "ui-avatars.com"],
  },
};

export default nextConfig;
